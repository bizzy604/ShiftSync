/**
 * @file /apps/web/src/components/AppLayout.tsx
 *
 * @description
 * Shared UI/component module for `AppLayout` used across multiple screens.
 *
 * @dependencies
 * - (No in-repo dependents detected.)
 *
 * @importance
 * This module provides reusable UI primitives that influence consistency and
 * maintainability.
 */

import { AppLayout } from './NavBar';

export { AppLayout };
